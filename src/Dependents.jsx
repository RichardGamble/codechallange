import React, { Component } from 'react';
import PropTypes from 'prop-types';

const Dependents = (props) => {
	return <div>Dependents List</div>;
};

Dependents.propTypes = {};

export default Dependents;
