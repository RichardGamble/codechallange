import React from 'react';
import PropTypes from 'prop-types';

const Employee = props => {
    return (
        <div>
            Test
        </div>
    );
};

Employee.propTypes = {
    
};

export default Employee;