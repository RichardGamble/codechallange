import React from 'react';
import PropTypes from 'prop-types';

const Paychecks = props => {
    return (
        <div>
            {/* displays table similar to the employees and dependens
             that lists all the paychecks, in descending order most recent first */}
        </div>
    );
};

Paychecks.propTypes = {
    
};

export default Paychecks;