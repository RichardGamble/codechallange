import React from 'react';
import PropTypes from 'prop-types';

const PaycheckModal = props => {
    return (
        <div>
            {/* Modal prepopulated with dependents, 
            the amount of discount they get and the total cost of the deduction.

            the paycheck has it's own table with a table(deductions) tied to it. */}
        </div>
    );
};

PaycheckModal.propTypes = {
    
};

export default PaycheckModal;